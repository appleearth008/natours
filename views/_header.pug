header.header
    nav.nav.nav--tours
        a.nav__el(href='/') All tours
    //- div.header__logo equals to .header__logo
    .header__logo
        //- without the /, this will load img from the 127.0.0.1:300/tour,
        //- so will be loaded like this: 127.0.0.1:300/tour/img/logo-white.png
        //- which is not correct. We want to load the img from the root url, which is 127.0.0.1:3000,
        //- which means, we should add a / before img, to change src='img/logo-white.png' to src='/img/logo-white.png',
        //- and this will load image relative to the root path (which is 127.0.0.1:3000)
        img(src='/img/logo-white.png' alt='Natours logo')
    nav.nav.nav--user
        if user
            a.nav__el.nav__el--logout Log Out
            a.nav__el(href='/me')
                img.nav__user-img(src=`/img/users/${user.photo}` alt=`Photo of ${user.name}`)
                span= user.name.split(' ')[0]
        else
            a.nav__el(href='/login') Log in 
            a.nav__el.nav__el--cta(href='#') Sign up