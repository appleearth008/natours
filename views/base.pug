doctype html 
html 
    head 
        block head 
            meta(charset='UTF-8')
            meta(name='viewport' content='width=device-width, initial-scale=1.0')
            //- this #{tour} is called the interpolation 
            title Natours | #{title}
            //- seems like I can use either '' or ""
            //- the css folder comes from the public folder, because in app.js, we serve the static files in the public folder 
            //- we used this: app.use(express.static(path.join(__dirname, 'public')));
            //- Also, this () means attributes in html code
            link(rel='stylesheet' href='/css/style.css') 
            link(rel='shortcut icon' type='img.png' href ='/img/favicon.png')
            link(rel='stylesheet', href='https://fonts.googleapis.com/css?family=Lato:300,300i,700')
    body 
        //- {h1= tour} this is called the buffer code, we can write javascript code here as well
        //- h1= tour 
        //- h2= user.toUpperCase()
        //- write javascript code after the -
        //- - const x = 9
        //- h2= x * 2
        //- h1 The Park Camper 
        //- p This is just some text.
        // Header
        //- the first two are classes in pug: 
        //- originally in html is: <header class="header"> <nav class="nav nav--tours">
        include _header

        // Content
        block content 
            h1 This is a placeholder heading!
        
        // Footer
        include _footer
        
        //- we always put our javascript file at the end? so that the DOM can be fully created if we want to access it in out javasript file
        //- question? why it isn't src="/public/js/mapbox.js"
        //- script(src='https://cdnjs.cloudflare.com/ajax/libs/axios/0.21.4/axios.min.js')
        //- script(src='/js/mapbox.js')
        //- script(src='/js/login.js')
        script(src='/js/bundle.js')
